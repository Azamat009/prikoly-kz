<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Приколы KZ</title>
    {{ encore_entry_link_tags('app') }}
</head>
<body>
<nav class="main-menu">
    <div class="dropdown">
        <button class="menu-btn">☰</button>
        <div class="dropdown-content">
            <a href="#" data-modal-target="#feedback-modal">Связаться</a>
            <a href="#" data-modal-target="#report-modal">Пожаловаться</a>
        </div>
    </div>
</nav>

<main id="video-feed" class="video-container"></main>

<!-- Модальные окна -->
<div id="feedback-modal" class="modal">
    <div class="modal-content">
        {{ render(controller('App\\Controller\\MenuController::feedback')) }}
    </div>
</div>

<div id="report-modal" class="modal">
    <div class="modal-content">
        {{ render(controller('App\\Controller\\MenuController::report')) }}
    </div>
</div>

{{ encore_entry_script_tags('app') }}
</body>
</html>